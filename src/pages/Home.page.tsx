import React from "react";
import BoardList from "../components/BoardList/BoardList.component";

const Home: React.FC = () => {
    return (
        <div>
            <BoardList />
        </div>
    );
};

export default Home;
